<meta charset="utf-8">
<title>Customer page</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="">
<meta name="author" content="">
<!-- Bootstrap styles -->
<link href=".././assets/css/bootstrap.css" rel="stylesheet"/>
<!-- Customize styles -->
<link href=".././style.css" rel="stylesheet"/>
<!-- font awesome styles -->
<link href=".././assets/font-awesome/css/font-awesome.css" rel="stylesheet">
<!--[if IE 7]>
<link href=".././css/font-awesome-ie7.min.css" rel="stylesheet">
<![endif]-->

<!--[if lt IE 9]>
<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
<![endif]-->

<!-- Favicons -->
<link rel="shortcut icon" href="assets/ico/favicon.ico">
<script>
    function successLogin() {

        alert("Đăng nhập thành công!");
    }

    function successRegister() {
        alert("Đăng ký thành công!");
    }

    function successUpdateAccount() {
        alert("Cập nhật thành công!");
    }

    function successShip() {
        alert("Cập nhật thành công!");
    }

    function successSearch() {
        alert("Tìm thành công!");
    }
    function successNewPassword() {
        alert("Mật khẩu mới đã được gửi tới mail của bạn!");
    }

    
    function changeIn4() {
        document.getElementById('inputName').readOnly = false;
        document.getElementById('inputAddress').readOnly = false;
        document.getElementById('inputPhone').readOnly = false;

        const nodeParent = document.getElementById("control-btn");

        const buttonSubmit = document.createElement("button");
        buttonSubmit.setAttribute("class", "exclusive shopBtn");
        buttonSubmit.setAttribute("type", "submit");
        buttonSubmit.setAttribute("form", "form-change-info");
        buttonSubmit.style.marginRight= "10px";
        buttonSubmit.innerText = "Đồng ý";

        const cancelBtn = document.createElement("button");
        cancelBtn.setAttribute("class", "exclusive shopBtn");
        cancelBtn.onclick = cancelChange;
        cancelBtn.style.marginRight="10px";
        cancelBtn.setAttribute("type", "reset");
        cancelBtn.innerText = "Hủy";

        const changePass = document.createElement("a");
        changePass.setAttribute("href", "/users/change_password");
        changePass.innerText = "Đổi mật khẩu";

        nodeParent.appendChild(buttonSubmit);
        nodeParent.appendChild(cancelBtn);
        nodeParent.appendChild(changePass);

        document.getElementById('changeIn4').style.display = 'none';
    }

    function cancelChange(){
        window.location.reload();gt
    }
</script>