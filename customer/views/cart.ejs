<!DOCTYPE html>
<html lang="en">
<head>
    <% include elements/tag_head.ejs%>

</head>
<body onload="loadItem()">
<!--
	Upper Header Section
-->
<% include ./elements/upper_header.ejs%>

<!--
Lower Header Section
-->
<div class="container">
    <% include elements/lower_header.ejs%>


    <!--
    Navigation Bar Section
    -->
    <% include elements/navbar.ejs%>
    <!--
    Body Section
    -->
    <div class="row">
        <div class="span12">
            <ul class="breadcrumb">
                <li><a href="/">Trang chủ</a> <span class="divider">/</span></li>
                <li class="active">Giỏ</li>
            </ul>
            <div class="well well-small">
                <%let num=0;
                for (let i=0;i<number.length;i++){
                    num+=number[i];
                }%>
                <h1>Thông tin giỏ hàng<small class="pull-right"> <%=num%> sản phẩm</small></h1>
                <hr class="soften"/>

                <table class="table table-bordered table-condensed">
                    <thead>
                    <tr>
                        <th>Sản phẩm</th>
                        <th>Tên sản phẩm</th>
                        <th>Mô tả</th>
                        <th>Tình trạng</th>
                        <th>Đơn giá</th>
                        <th>Số lượng </th>
                        <th>Tổng tiền</th>
                    </tr>
                    </thead>
                    <tbody>
                    <% let i=0; let sum=0;
                    product_list.forEach(function (product) {%>
                    <tr>
                        <td><img width="100" src="<%= product.thumbnail%>" alt=""></td>
                        <td><%= product.name%></td>
                        <td><%= product.description%></td>
                        <td><%= product.status%></td>
                        <td><%= product.price.toString().replace(/(\d)(?=(\d{3})+(?!\d))/g, '$1,') +"đ"%></td>
                        <td>
                        <input class="span1" style="max-width:34px" placeholder="1" id="appendedInputButtons" size="16" type="text" value="<%= number[i]%>">
                                <div class="input-append">
                                <button class="btn btn-mini" type="button">-</button><button class="btn btn-mini" type="button"> + </button><button class="btn btn-mini btn-danger" type="button"><span class="icon-remove"></span></button>
                        </div>
                        </td>
                        <td><%= (product.price*number[i]).toString().replace(/(\d)(?=(\d{3})+(?!\d))/g, '$1,') +"đ"%></td>
                    </tr>
                    <%sum+=product.price*number[i++];});%>

                    <tr>
                        <td colspan="6" class="alignR sumPrice">Tổng tiền:	</td>
                        <td class="sumPrice"><%=sum.toString().replace(/(\d)(?=(\d{3})+(?!\d))/g, '$1,') +"đ";%></td>
                    </tr>
                    </tbody>
                </table><br/>


                <table class="table table-bordered">
                    <tbody>
                    <tr>
                        <td>
                            <form class="form-inline">
                                <label style="min-width:159px"> VOUCHERS Code: </label>
                                <input type="text" class="input-medium" placeholder="CODE">
                                <button type="submit" class="shopBtn"> Thêm</button>
                            </form>
                        </td>
                    </tr>

                    </tbody>
                </table>
                <a href="/products" class="shopBtn btn-large"><span class="icon-arrow-left"></span> Tiếp tục mua hàng </a>
                <%let ship="/users/login";
                if (userdata)
                {
                    ship='/ship';
                }%>
                <a href="<%=ship%>" class="shopBtn btn-large pull-right">Tiếp <span class="icon-arrow-right"></span></a>

            </div>
        </div>
    </div>
    <!--
    Clients
    -->
    <% include elements/client.ejs%>

    <!--
    Footer
    -->
    <% include elements/footer.ejs%>
</div><!-- /container -->

<% include elements/copyright.ejs%>
<a href="#" class="gotop"><i class="icon-double-angle-up"></i></a>
<!-- Placed at the end of the document so the pages load faster -->
<% include elements/script_end.ejs%>
</body>
</html>
